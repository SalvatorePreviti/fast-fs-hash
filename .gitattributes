# Force LF line endings everywhere — this project is cross-platform and
# all build/test tooling expects Unix-style line endings.
* text=auto eol=lf

# Test fixtures: treat as binary so git never modifies their content.
# Hash tests depend on exact byte-for-byte content.
test/fixtures/** binary
